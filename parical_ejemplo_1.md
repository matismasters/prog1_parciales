# Ejemplos ejercicios de parcial

El parcial va a durar 2 horas, y va a ser un exámen práctico en computadora. Se podrá tener cualquier material impreso, y se podrá acceder via internet únicamente a los siguientes materiales trabajados en clase:

- [https://github.com/matismasters/prog1_js_basico](https://github.com/matismasters/prog1_js_basico)
- [https://github.com/matismasters/prog1_html](https://github.com/matismasters/prog1_html)
- [https://github.com/matismasters/prog1_oop](https://github.com/matismasters/prog1_oop)
- [https://github.com/matismasters/prog1_cheatsheets](https://github.com/matismasters/prog1_cheatsheets)
- [https://github.com/matismasters/prog1_swapscore](https://github.com/matismasters/prog1_swapscore)

No se permite el uso de teléfonos móviles durante el examen.

El parcial llevará 6 ejercicios similares a los siguientes:

## Ejercicio 69

Crea una función llamada `contarPares` que recibe un array de números y devuelve la cantidad de números pares que tiene el array.

```javascript

```

## Ejercicio 70

Crea una función llamada `crearLiHTML` que recibe un string como parámetro y devuelve un elemento `li` de HTML con el texto recibido como contenido.

```javascript

```

## Ejercicio 71

Crea una función llamada `devolverMínimo` que recibe un array de números y devuelve el número más pequeño del array.

```javascript

```

## Ejercicio 72

Crea una función llamada `masIVA` que recibe un número como parámetro y devuelve el número sumando el 22% del IVA.

```javascript

```

## Ejercicio 73

Crea una función que recibe un array de strings y devuelve un array de elementos `h1` de HTML con los textos recibidos como contenido.

```javascript

```

## Ejercicio 74

Crea 2 funciones, una función que captura un elemento del documento HTML con el id `botonBuscar` y le asigna la función `buscar` al evento `click`; La función `buscar` debe mostrar un mensaje al usuario diciendo "Buscando...".

```javascript

```

## Ejercicio 75

Crea una clase `Ciudad` con 3 propiedas, 2 métodos de instancia y un método estático. Uno de los métodos de instancia debe llamar al otro. El método estatico debe mostrar un mensaje en consola.

```javascript

```

## Ejercicio 76

Crea una función que recibe un objecto, por ejemplo `{ nombre: "pedro", apellido: "pe" }`, y lo guarda en el `localstorage` con la clave "usuario".

```javascript

```

## Ejercicio 77

Crea una función que trae el contenido del `localstorage` de la clave "usuario" y lo muestra en consola.

```javascript

```

## Ejercicio 78

Crea una función que recibe un array de strings como parámetro y devuelve, en un array, aquellos elemenots del array recibido como parámetro que que contienen la frase "promoción". Utilizar el método `filter`.

```javascript

```

Claro, aquí tienes diez ejercicios adicionales siguiendo el mismo formato y temas:

## Ejercicio 79

Crea una función llamada `filtrarImpares` que recibe un array de números y devuelve un nuevo array con solo los números impares.

```javascript

```

## Ejercicio 80

Crea una función llamada `crearDivHTML` que recibe un string como parámetro y devuelve un elemento `div` de HTML con el texto recibido como contenido.

```javascript

```

## Ejercicio 81

Crea una función llamada `sumarArray` que recibe un array de números y devuelve la suma de todos los números del array.

```javascript

```

## Ejercicio 82

Crea una función llamada `menosIVA` que recibe un número como parámetro y devuelve el número restando el 22% del IVA.

```javascript

```

## Ejercicio 83

Crea una función que recibe un array de strings y devuelve un array de elementos `p` de HTML con los textos recibidos como contenido.

```javascript

```

## Ejercicio 84

Crea 2 funciones, una función que captura un elemento del documento HTML con el id `botonEnviar` y le asigna la función `enviar` al evento `click`; La función `enviar` debe mostrar un mensaje al usuario diciendo "Enviando...".

```javascript

```

## Ejercicio 85

Crea una clase `Producto` con 3 propiedades, 2 métodos de instancia y un método estático. Uno de los métodos de instancia debe calcular el precio con IVA y el otro debe mostrar el precio con IVA. El método estático debe mostrar un mensaje en consola.

```javascript

```

## Ejercicio 86

Crea una función que recibe un objeto, por ejemplo `{ titulo: "El Quijote", autor: "Cervantes" }`, y lo guarda en el `localstorage` con la clave "libro".

```javascript

```

## Ejercicio 87

Crea una función que trae el contenido del `localstorage` de la clave "libro" y lo muestra en consola.

```javascript

```

## Ejercicio 88

Crea una función que recibe un array de strings como parámetro y devuelve, en un array, aquellos elementos del array recibido como parámetro que contienen la palabra "descuento". Utilizar el método `filter`.

```javascript

```

## Ejercicio 89

Crea una clase `Barco` con 3 propiedades, 2 métodos de instancia y un método estático. Uno de los métodos de instancia debe calcular la velocidad y el otro debe mostrar la velocidad. El método estático debe mostrar un mensaje en consola.

```javascript

```

## Ejercicio 90

Crea una clase `Escoba` con 3 propiedades, 2 métodos de instancia y un método estático. Uno de los métodos de instancia debe calcular la velocidad y el otro debe mostrar la velocidad. El método estático debe mostrar un mensaje en consola.

```javascript

```

## Ejercicio 91

Crea una clase `Libro` con 3 propiedades, 2 métodos de instancia y un método estático. Uno de los métodos de instancia debe calcular el precio con IVA y el otro debe mostrar el precio con IVA. El método estático debe mostrar un mensaje en consola.

```javascript

```

## Ejercicio 92

Crea una clase `HerramientasDeManejoDeString` sin ninguna propiedad, pero con los siguientes métodos estaticos:

- `reemplazarVocalesPorAsteriscos`: recibe un string y devuelve el mismo string reemplazando todas las vocales por asteriscos.

- `pasarAMayusculas`: recibe un string y devuelve el mismo string en mayúsculas.

- `pasarAMinusculas`: recibe un string y devuelve el mismo string en minúsculas.

```javascript

```

## Ejercicio 93

Crea una clase `HerramientasDeManejoDeArray` sin ninguna propiedad, pero con los siguientes métodos estaticos:

- `restarTodoA`: recibe dos paramétros `totalVentas`, que es un número, y `totalDescuentos`, que es un array de números, y devuelve el resultado de restarle a `totalVentas` todos los descuentos de `totalDescuentos`.

- `filtrarStockPositivo`: recibe un array de objectos con el siguiente formato `{ nombre: "producto", stock: 10 }`, y devuelve un array con solo aquellos productos que tienen stock positivo.

## Ejercicio 94

Crea una clase `TemaMusical` con las siguientes propiedades:

- `nombre`
- `autor`
- `duracionEnSegundos`
- `genero`

Con los siguientes métodos de instancia:

- `duracionEnMinutos`: devuelve la duración en minutos del tema.
- `duracionEnHoras`: devuelve la duración en horas del tema.

Con los siguientes métodos estáticos:

- `filtrarPorGenero`: recibe un array de instancias de temas musicales y un género, y devuelve un array con solo aquellos temas que son del género recibido.

- `filtrarPorDuracion`: recibe un array de instancias de temas musicales y una duración en segundos, y devuelve un array con solo aquellos temas que duran más de la duración recibida.

- `duracionTotal`: recibe un array de instancias de temas musicales y devuelve la duración total de todos los temas en segundos.

## Ejercicio 95

Crea una clase `Cine` con 5 propiedades, 2 métodos de instancia y un método estático. Uno de los métodos de instancia debe calcular la cantidad de butacas ocupadas y el otro debe mostrar la cantidad de butacas ocupadas. El método estático debe mostrar un mensaje en consola.

## Ejercicio 96

Crea una clase `Jugador` con 5 propiedades, 2 métodos de instancia y un método estático. Uno de los métodos de instancia debe ser `esGoleador` y debe devolver `true` si el jugador ha hecho mas de 100 goles. El otro método de instancia debe ser `esHorrible` y debe devolver `true` si el jugador tiene un ratio de acierto de pases menor al 10%.

## Ejercicio 97

Crea una clase `Celebridad` con 5 propiedades, 2 métodos de instancia y un método estático. Uno de los métodos de instancia debe ser `esFamoso` y debe devolver `true` si la celebridad tiene mas de 1 millón de seguidores. El otro método de instancia debe ser `esInsoportable` y debe devolver `true` si la celebridad tiene mas de 1000 comentarios negativos en sus redes sociales.

## Ejercicio 98

Crea 2 clases, una llamada `Factura` y otra llamada `ItemFactura`. La clase `ItemFactura` debe tener 2 propiedades, `nombre` y `precio`, y la clase `Factura` debe tener dos propiedades, `codigo`, que es un string, e `items` que es un array de instancias de `ItemFactura`. La clase `Factura` debe tener un método de instancia llamado `calcularTotal` que devuelve la suma de los precios de todos los items de la factura. La clase `Factura` debe tener métodos de instancia para agregar y quitar items de la factura. 

## Ejercicio 99

Crea 2 clases, una llamada `CuentaBancaria` y otra llamada `Movimiento`. La clase `Movimiento` debe tener 2 propiedades, `fecha` y `monto`, y la clase `CuentaBancaria` debe tener dos propiedades, `numero`, que es un string, e `items` que es un array de instancias de `Movimiento`. La clase `CuentaBancaria` debe tener un método de instancia llamado `calcularSaldo` que devuelve la suma de los montos de todos los movimientos de la cuenta. La clase `CuentaBancaria` debe tener métodos de instancia para agregar y quitar movimientos de la cuenta. La clase `CuentaBancaria` debe tener un método de instancia llamado `calcularSaldoEnFecha` que recibe una fecha y devuelve el saldo de la cuenta en esa fecha.


## Ejercicio 100

Crea 2 clases, una llamada `Juguete` y otra llamada `Juguetería`. La clase `Juguetería` debe tener 4 propiedades, `nombre`, que es un string, `direccion` que es un string, `RUT` que es un string, e `items` que es un array de instancias de `Juguete`. La clase `Juguetería` debe tener un método de instancia llamado `calcularTotal` que devuelve la suma de los precios de todos los juguetes de la juguetería. La clase `Juguetería` debe tener métodos de instancia para agregar y quitar juguetes de la juguetería.
